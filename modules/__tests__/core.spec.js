import 'js-polyfills'
import expect from 'expect'

describe('Rest', () => {
  describe('core', () => {
    describe('buildURL', () => {
      it('creates a url with an ordinary path', () => {
      })
  })
})
